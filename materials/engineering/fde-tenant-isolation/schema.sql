-- Unsolved. Implement only in your disposable padipps_fde_* database.
-- Required: fde_contacts table, composite identity, restricted app privileges,
-- RLS read/write policies, and deny-by-default transaction tenant context.
DO $$ BEGIN
  RAISE EXCEPTION 'TODO learner implementation: tenant schema and policies';
END $$;
